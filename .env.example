# Optional but recommended: free Vnstock Community key (higher request limit than Guest)
VNSTOCK_API_KEY=

# Optional production persistence. The engine still runs without these.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
